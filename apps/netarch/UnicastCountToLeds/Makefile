COMPONENT=UnicastCountToLedsAppC

CFLAGS += -DDELUGE_BASESTATION

CFLAGS += -DTOSH_DATA_LENGTH=36
CFLAGS += -DDSR_ROUTE_CACHE_MAX_NODES=20
CFLAGS += -DDSR_ROUTE_CACHE_MAX_LINKS=50
CFLAGS += -DPACKET_ENGINE_SEND_SIZE=10
CFLAGS += -DPACKET_ENGINE_RECEIVE_SIZE=10

TOSCONTRIBRICE = ../../..
CFLAGS += `find $(TOSCONTRIBRICE)/tos -type d -a \( -wholename *.svn* -prune -o -print \) | sed -r 's/.*/-I&/'`

include $(MAKERULES)

